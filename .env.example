# Supabase (leave unset to run the local demo store)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Demo-only warden password. Ignored unless the app runs in demo mode
# (i.e. Supabase is not configured). Never set this in production.
VITE_DEMO_WARDEN_PASSWORD=
