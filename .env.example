# Supabase (leave unset to run the local demo store)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Demo-only credentials. Ignored unless the app runs in demo mode
# (i.e. Supabase is not configured). Never set these in production.
VITE_DEMO_OTP=
VITE_DEMO_WARDEN_PASSWORD=
